# Input Hub

Raw, unclassified, or candidate materials may enter through this surface when
Alpha wants a local staging area before creating governed work.

For immediate live operations, approved work may be created directly under an
active parent in `03_portfolios/` without using the hub.

## Lifecycle

`raw_drop -> triage -> candidate_* -> pending_admission -> promoted_log | rejected_or_returned`
