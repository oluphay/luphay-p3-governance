# Projects

Projects housed under the program live here.

## Rules

- Each project home contains a primary `P3-PROJ-*.md` file and sibling `entity.yaml`.
- Task packets are nested inside the project's `task-packets/` folder.
- Project-owned artifacts and project-scoped decisions remain inside the project home.
